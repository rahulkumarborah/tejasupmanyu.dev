import { rhythm, scale } from "../../utils/typography"

export const rootHeaderStyles = {
  ...scale(1),
  marginBottom: 0,
  marginTop: 0,
  color: "primary",
}

export const headerStyles = {
  ...scale(0.75),
  fontFamily: `Montserrat, sans-serif`,
  margin: 0,
}

export const miniHeaderStyles = {
  ...scale(0.6),
  fontFamily: `Montserrat, sans-serif`,
  margin: 0,
}

export const linkStyles = {
  boxShadow: `none`,
  textDecoration: `none`,
  color: `inherit`,
}
